# Database Configuration
DATABASE_FILE=data/development.db

# DATABASE_URL is relative to where you run cargo commands (backend/ directory)
DATABASE_URL=sqlite:data/development.db

# Backup Configuration (used in production)
BACKUP_DIR=data/backups

# Logging
RUST_LOG=backend=debug,sqlx=info
