# =============================================================================
# Local Development (running cargo commands directly)
# =============================================================================

# Database file path (relative to backend/ directory)
DATABASE_FILE=data/development.db

# Database URL for SQLx (relative to backend/ directory)
DATABASE_URL=sqlite:data/development.db

# Backup directory (relative to backend/ directory)
BACKUP_DIR=data/backups

# Logging level
RUST_LOG=backend=debug,sqlx=info

# =============================================================================
# Docker Compose Configuration
# =============================================================================

# Host directory to mount as /app/data (contains library.db)
# Laptop: ./data (relative to docker-compose.yaml)
# VM example: /home/user/library/data
DATA_DIR=./data

# Host directory to mount as /app/backups
# Laptop: ./backups (relative to docker-compose.yaml)
# VM example: /mnt/nas/backups
BACKUPS_DIR=./backups
