# GitHub webhook secret - generate with: openssl rand -hex 32
# This MUST match the secret configured in GitHub webhook settings
WEBHOOK_SECRET=your-secret-here

# Optional: set repo root for deploy.sh (defaults to webhook working directory)
# REPO_DIR=/repo
