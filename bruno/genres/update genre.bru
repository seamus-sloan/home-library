meta {
  name: Update Genre
  type: http
  seq: 4
}

put {
  url: {{BASE_URL}}/genres/{{GENRE_ID}}
  body: json
  auth: none
}

headers {
  currentUserId: {{USER_ID}}
}

body:json {
  {
    "name": "Updated Genre Name",
    "color": "#e74c3c"
  }
}
